{
  "optimizer": "adam",
  "learning_rate": 0.001,
  "weight_decay": 0.0,
  "dropout": 0.1,
  "embedding_dim": 128,
  "epochs": 60,
  "metadata": {
    "last_updated_round": 2,
    "updated_by": "investigator",
    "reason": "Three complementary changes from 0.9297 baseline: embedding_dim 64→128 (proven scaling), epochs 40→60 (still converging at ep40), learning_rate 0.01→0.001 (finer steps near optimum, LR never reduced in 120 prior epochs)"
  }
}
